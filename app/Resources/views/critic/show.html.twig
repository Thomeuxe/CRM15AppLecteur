<div>
    <blockquote>
        {% for i in 1..5 %}
            <span class="glyphicon glyphicon-star{% if i > critic.score %}-empty{% endif %}" aria-hidden="true"></span>
        {% endfor %}
        <p>{{ critic.content }}</p>
        <footer>Publié par <cite title="Source Title"><a href="{{ path('review_user', {'username': critic.user.username}) }}">{{ critic.user.username }}</a></cite></footer>
    </blockquote>
</div>
